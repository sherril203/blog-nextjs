import React from 'react'
import Login from '../../components/login'

export const metadata = {
  title: "login page",
  description: "login to page with credientials",
};
const page = () => {
  return (
    <div>
      <Login/>
    </div>
  )
}

export default page
